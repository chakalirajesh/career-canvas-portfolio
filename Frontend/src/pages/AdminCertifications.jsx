import { useEffect,useState } from "react"; import "./AdminCertifications.css";
function AdminCertifications(){const [items,setItems]=useState([]);const [loading,setLoading]=useState(true);const load=async()=>{try{const r=await fetch("http://localhost:8080/certifications");if(!r.ok)throw new Error();setItems(await r.json())}catch(e){console.error(e)}finally{setLoading(false)}};useEffect(()=>{if(sessionStorage.getItem("adminLoggedIn")!=="true"){window.location.href="/admin/login";return}load()},[]);const del=async id=>{if(!confirm("Delete this certification?"))return;try{const r=await fetch(`http://localhost:8080/certifications/${id}`,{method:"DELETE",credentials:"include"});if(!r.ok)throw new Error();setItems(v=>v.filter(x=>x.id!==id))}catch(e){alert("Unable to delete certification")}};return <div className="admin-certifications-page"><header className="admin-certifications-header"><div><span className="admin-certifications-label">ADMIN</span><h1>Manage Certifications</h1><p>Manage certificates and credentials.</p></div><div className="admin-certifications-actions"><button className="primary" onClick={()=>window.location.href="/admin/certifications/add"}>+ Add Certification</button><button onClick={()=>window.location.href="/admin/dashboard"}>Dashboard</button></div></header><main className="admin-certifications-content">{loading?<p className="admin-certifications-status">Loading certifications...</p>:items.length===0?<p className="admin-certifications-status">No certifications found.</p>:<div className="admin-certifications-grid">{items.map(x=><div className="admin-certifications-card" key={x.id}><span className="admin-certifications-number">#{x.id}</span><h2>{x.name}</h2><h3>{x.issuer}</h3><p>Issued: {x.issueDate}</p><div className="admin-certifications-meta">{x.credentialUrl||"No credential URL"}</div><div className="admin-certifications-buttons"><button className="admin-certifications-edit" onClick={()=>window.location.href=`/admin/certifications/edit/${x.id}`}>Edit</button><button className="admin-certifications-delete" onClick={()=>del(x.id)}>Delete</button></div></div>)}</div>}</main></div>}
export default AdminCertifications;
