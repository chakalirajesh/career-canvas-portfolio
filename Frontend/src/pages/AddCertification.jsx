import { useState } from "react"; import "./AddCertification.css";
function AddCertification(){const [name,setName]=useState("");const [issuer,setIssuer]=useState("");const [issueDate,setIssueDate]=useState("");const [credentialUrl,setCredentialUrl]=useState("");const [message,setMessage]=useState("");const submit=async e=>{e.preventDefault();try{const r=await fetch("http://localhost:8080/certifications",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({name,issuer,issueDate,credentialUrl})});const d=await r.json();if(!r.ok)throw new Error(d.message||"Failed to add certification");setMessage("Certification added successfully.");setName("");setIssuer("");setIssueDate("");setCredentialUrl("")}catch(e){setMessage(e.message)}};return <div className="add-certification-page"><div className="add-certification-header"><div><span>ADMIN</span><h1>Add Certification</h1><p>Add a new certification.</p></div><button className="add-certification-back" onClick={()=>window.location.href="/admin/certifications"}>Back</button></div><form className="add-certification-form" onSubmit={submit}><div className="add-certification-group"><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} required/></div><div className="add-certification-group"><label>Issuer</label><input value={issuer} onChange={e=>setIssuer(e.target.value)} required/></div><div className="add-certification-group"><label>Issue Date</label><input value={issueDate} onChange={e=>setIssueDate(e.target.value)} placeholder="2026-01" required/></div><div className="add-certification-group"><label>Credential URL</label><input type="url" value={credentialUrl} onChange={e=>setCredentialUrl(e.target.value)}/></div><button className="add-certification-submit" type="submit">Add Certification</button>{message&&<p className="add-certification-message">{message}</p>}</form></div>}
export default AddCertification;
