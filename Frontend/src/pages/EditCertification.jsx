import { useEffect,useState } from "react"; import "./EditCertification.css";
function EditCertification({certificationId}){const [name,setName]=useState("");const [issuer,setIssuer]=useState("");const [issueDate,setIssueDate]=useState("");const [credentialUrl,setCredentialUrl]=useState("");const [message,setMessage]=useState("");useEffect(()=>{if(sessionStorage.getItem("adminLoggedIn")!=="true"){window.location.href="/admin/login";return}(async()=>{try{const r=await fetch(`https://career-canvas-portfolio.onrender.com//certifications/${certificationId}`);if(!r.ok)throw new Error("Certification not found");const d=await r.json();setName(d.name||"");setIssuer(d.issuer||"");setIssueDate(d.issueDate||"");setCredentialUrl(d.credentialUrl||"")}catch(e){setMessage(e.message)}})()},[certificationId]);const submit=async e=>{e.preventDefault();try{const r=await fetch(`http://localhost:8080/certifications/${certificationId}`,{method:"PUT",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({name,issuer,issueDate,credentialUrl})});const d=await r.json();if(!r.ok)throw new Error(d.message||"Failed to update certification");setMessage("Certification updated successfully.")}catch(e){setMessage(e.message)}};return <div className="edit-certification-page"><div className="edit-certification-header"><div><span>ADMIN</span><h1>Edit Certification</h1><p>Update certification information.</p></div><button className="edit-certification-back" onClick={()=>window.location.href="/admin/certifications"}>Back</button></div><form className="edit-certification-form" onSubmit={submit}><div className="edit-certification-group"><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} required/></div><div className="edit-certification-group"><label>Issuer</label><input value={issuer} onChange={e=>setIssuer(e.target.value)} required/></div><div className="edit-certification-group"><label>Issue Date</label><input value={issueDate} onChange={e=>setIssueDate(e.target.value)} required/></div><div className="edit-certification-group"><label>Credential URL</label><input type="url" value={credentialUrl} onChange={e=>setCredentialUrl(e.target.value)}/></div><button className="edit-certification-submit" type="submit">Update Certification</button>{message&&<p className="edit-certification-message">{message}</p>}</form></div>}
export default EditCertification;
